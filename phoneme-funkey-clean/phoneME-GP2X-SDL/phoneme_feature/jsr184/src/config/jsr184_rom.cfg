# JSR-184 Mobile 3D Graphics classes.
#
# Keep init-at-build empty while the native backend is under active porting.
# The classes are safe to load only after the KNI layer and FunKey renderer are
# linked into the VM.

